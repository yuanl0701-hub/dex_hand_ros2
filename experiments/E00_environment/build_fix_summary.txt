Record status: historical failure evidence

Failure category:
ROS 2 Humble build-structure incompatibility

Observed result:
CMake duplicate custom target error.

Root cause:
The same package generated ROS interfaces and installed a Python package
under the identical package name.

Corrective action:
The source tree was subsequently changed to move ROS interfaces into the
separate dex_hand_interfaces package.

Verification status:
Not verified by the historical files in this directory. Run the automated
thesis experiment suite on Ubuntu and use its post-fix E00 outputs before
claiming successful or reproducible builds.
